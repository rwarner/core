{
  "config": {
    "abort": {
      "install_app": "Open the mobile app to set up the integration with Home Assistant. See [the docs]({apps_url}) for a list of compatible apps."
    },
    "step": {
      "confirm": {
        "description": "Do you want to set up the Mobile App component?"
      }
    }
  },
  "device_automation": {
    "action_type": {
      "notify": "Send a notification"
    },
    "extra_fields": {
      "message": "Message",
      "title": "Title"
    }
  },
  "exceptions": {
    "device_not_connected_for_local_push_notifications": {
      "message": "Device {device_name} is not connected for local push notifications"
    },
    "error_sending_notification": {
      "message": "Error sending notification to {device_name}"
    },
    "live_activity_unknown_device": {
      "message": "No mobile_app device found for device_id {device_id}"
    },
    "rate_limit_exceeded_sending_notification": {
      "message": "Rate limit exceeded sending notification to {device_name}"
    },
    "timeout_sending_notification": {
      "message": "Timeout sending notification to {device_name}"
    }
  },
  "services": {
    "set_live_activity_state": {
      "description": "Records the current Live Activity state for a tag. Success means the state was recorded; the device receives it on the next reachable dispatch.",
      "fields": {
        "device_id": {
          "description": "The mobile_app device whose Live Activity state is being set.",
          "name": "Device"
        },
        "tag": {
          "description": "Tag that uniquely identifies the Live Activity on the device.",
          "name": "Activity tag"
        },
        "content_state": {
          "description": "Content state dictionary delivered to the Live Activity widget. Overwrites any prior state for the same tag.",
          "name": "Content state"
        }
      },
      "name": "Set Live Activity state"
    }
  },
  "title": "Mobile App"
}
